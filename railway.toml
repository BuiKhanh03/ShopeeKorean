[build]
builder = "dockerfile"
dockerfilePath="./ShopeeKorean.Application/Dockerfile"

[deploy]
startCommand = "dotnet ShopeeKorean.dll"
restartPolicyType = "always"
healthcheckPath = "/health"
healthcheckTimeout = 120
